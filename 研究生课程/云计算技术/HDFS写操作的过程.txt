HDFS写操作的过程

（1）应用写入文件系统。
（2）客户机写入缓冲区，直到接收到64K的块。
（3）客户机通知名称节点（NameNode）它想写一个新的块。
（4）名称节点返回3个供写入的数据节点（DataNode）的名单。
（5）客户机发送块到第一个数据节点并通知其他两个副本（replica）的数据节点。
（6）第一个数据节点写入块并将其发送到第二个数据节点。第二个数据节点写入块并把它发给最后一个数据节点。
（7）每个数据节点在完成写入后向客户机报告。
（8）客户机在这3个数据节点报告完成写入后向名称节点确认写操作。
